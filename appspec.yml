version: 0.0
os: ubuntu
files:
  - source: /
    destination: /home/kevin/opt/laravel/
hooks:
  BeforeInstall:
    - location: deploy/beforeinstall.sh
      timeout: 300
      runas: root
  AfterInstall:
    - location: deploy/afterinstall.sh
      timeout: 300
      runas: root   
 


